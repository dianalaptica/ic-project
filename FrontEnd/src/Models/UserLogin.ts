export type UserLogin = {
    email: string;
    password: string;
    token: string;
    role: string;
}

export type UserProfile = {
    // firstName: string;
    // lastName: string;
    email: string;
    role: string;
}