export type UserLogin = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    token: string;
    role: string;
}

export type UserProfile = {
    firstName: string;
    lastName: string;
    email: string;
    role: string;
}
